<div class="form_div">
    <label for="{{$name}}">
        Descrição
    </label>

   <textarea name="{{$name}}" id="{{$name}}" cols="30" rows="10" placeholder="{{$placeholder}}">{{$value ?? null}}</textarea>

</div>
