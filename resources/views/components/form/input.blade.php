<div class="form_div">
    <label for="{{$name}}">
        {{$label ?? null}}
    </label>

    <input type="{{$type}}" name="{{$name}}" id="{{$name}}" placeholder="{{$placeholder ?? null}}" value="{{$value ?? null}}">

</div>
